1) Claude Codeに、Planモードを有効にした上で以下のプロンプトを入力。

入力:
```
在庫管理用のWebアプリケーションを開発したいです。開発を始める前に仕様を検討したので、仕様を提案してください。なお、運用費を抑えるために、RailwayとSQLiteを使いたいです。私一人で使用します。部品の管理がしたいです。まずは、在庫数の入出庫履歴の管理を作りたいです。技術に明るくないので、オススメを選んでください。スマホでも使いたいです。型番と単価も管理したいです。
```

2) Cluade Codeから以下の4点の質問を受け、それに対して選択肢から「→」の右側の項目を選択して回答。

回答および入力:
```
· ログイン認証はどうしますか？（一人利用ですが外部公開URLになるため何らかの保護を推奨） → パスワード1つで保護 (推奨)
· 部品マスタに持たせる項目はどれにしますか？（必須の「部品名/型番/単価」に加えて） → メーカー名, 保管場所, 最低在庫数（アラート用）, メモ/備考
· 入出庫履歴で記録したい項目は？（必須の「日時/種別(入/出)/数量/部品」に加えて） → 理由・メモ（自由記述）
· UIの言語は？ → 日本語
```

3) Claude Codeから以下のような実装計画を受け取ったので、承認して実装を開始。

回答:
```
Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
在庫管理Webアプリ 実装計画

Context（背景・目的）

個人で使う部品の在庫管理を行うWebアプリをゼロから構築する。スマホからも操作したい。Railway + SQLite で運用費を最小化する方針。リポジトリ inventory-manager は現在 .git のみで空。

実現したいこと:
- 部品マスタ（名前 / 型番 / 単価 / メーカー / 保管場所 / 最低在庫数 / メモ）の登録・編集
- 入出庫履歴（日時 / 種別(入/出) / 数量 / 部品 / 理由メモ）の追加
- 現在在庫の自動算出（履歴の集計）と、最低在庫を下回った部品のアラート表示
- スマホでも使えるレスポンシブUI、日本語
- パスワード1つによる簡易認証

 ---
技術スタック（推奨）

技術に詳しくない前提で、情報が多く・運用が簡単・将来の拡張も効くものを選定。

┌────────────────┬───────────────────────────────────────────────────────────────────────┬──────────────────────────────────────────────────┐
│      領域      │                                 採用                                  │                       理由                       │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ 言語           │ TypeScript                                                            │ 型安全。エディタ補完が効きミスが減る             │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ フレームワーク │ Next.js 15 (App Router)                                               │ フロント+APIが一体。学習リソース最多             │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ UI             │ Tailwind CSS v4 + shadcn/ui                                           │ モバイル対応容易・コピペで部品追加可能           │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ DB             │ SQLite (Railway Volume にファイル配置)                                │ 単一ユーザーで十分。バックアップはファイルコピー │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ ORM            │ Prisma                                                                │ スキーマ宣言→マイグレーション自動生成            │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ 認証           │ iron-session（Cookie に署名付きセッション）+ 環境変数の単一パスワード │ 1ユーザー用の最小構成                            │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ デプロイ       │ Railway (Nixpacks ビルド) + 永続Volumeに /data/inventory.db           │ 月数ドル以内・Volume で再デプロイ後もDB保持      │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ パッケージ管理 │ pnpm                                                                  │ Railwayで動作実績あり・速い                      │
├────────────────┼───────────────────────────────────────────────────────────────────────┼──────────────────────────────────────────────────┤
│ Lint/Format    │ Biome                                                                 │ ESLint+Prettier代替、設定1ファイル               │
└────────────────┴───────────────────────────────────────────────────────────────────────┴──────────────────────────────────────────────────┘

▎ 補足: Cloudflare D1 / Turso なども候補だが、ユーザー希望の「SQLite + Railway」に従い、Railway Volume にSQLiteファイルを置く構成にする。

 ---
データモデル（Prisma schema）

ファイル: prisma/schema.prisma

datasource db { provider = "sqlite"; url = env("DATABASE_URL") }
generator client { provider = "prisma-client-js" }

model Part {
id              String       @id @default(cuid())
name            String                              // 部品名
partNumber      String       @unique                // 型番
unitPrice       Int                                 // 単価（円・整数で保持）
manufacturer    String?                             // メーカー名
location        String?                             // 保管場所
minStock        Int          @default(0)            // 最低在庫数
note            String?                             // メモ
createdAt       DateTime     @default(now())
updatedAt       DateTime     @updatedAt
movements       StockMovement[]
}

model StockMovement {
id        String   @id @default(cuid())
partId    String
part      Part     @relation(fields: [partId], references: [id], onDelete: Cascade)
type      String                              // "IN" | "OUT"
quantity  Int                                 // 正の整数
reason    String?                             // 理由・メモ
occurredAt DateTime @default(now())           // 入出庫日時（編集可）
createdAt DateTime @default(now())
}

現在在庫は SUM(IN.quantity) - SUM(OUT.quantity) で算出（一覧表示時にSQLで集計）。在庫スナップショットテーブルは作らない（一人運用で件数も少ないため十分）。

 ---
画面構成（モバイルファースト）

すべて日本語。下部にタブナビゲーション（スマホ向け）/ 広い画面では左サイドバー。

1. /login — パスワード入力1つ
2. /（部品一覧）
- 検索（部品名・型番の部分一致）
- 各行: 部品名 / 型番 / 現在在庫 / 単価 / 最低在庫割れバッジ
- 行タップで詳細へ。右上に「＋部品を追加」
3. /parts/new、/parts/[id]/edit — 部品マスタ編集フォーム
4. /parts/[id]（部品詳細）
- 現在在庫、マスタ情報
- 「入庫」「出庫」の大きなボタン → モーダルで数量と理由メモを入力
- その部品の履歴を時系列表示
5. /history（全履歴）
- 期間・種別・部品でフィルタ
- 列: 日時 / 部品名(型番) / 種別 / 数量 / 理由

 ---
主要ファイル（作成予定）

inventory-manager/
├─ package.json
├─ pnpm-lock.yaml
├─ next.config.ts
├─ tsconfig.json
├─ biome.json
├─ .env.example                # APP_PASSWORD, SESSION_SECRET, DATABASE_URL
├─ railway.json                # Volume を /data にマウント
├─ prisma/
│  └─ schema.prisma
├─ src/
│  ├─ app/
│  │  ├─ layout.tsx            # 全体レイアウト＋ナビ
│  │  ├─ login/page.tsx
│  │  ├─ page.tsx              # 部品一覧
│  │  ├─ parts/new/page.tsx
│  │  ├─ parts/[id]/page.tsx
│  │  ├─ parts/[id]/edit/page.tsx
│  │  ├─ history/page.tsx
│  │  └─ api/
│  │     ├─ login/route.ts
│  │     ├─ logout/route.ts
│  │     ├─ parts/route.ts            # GET 一覧 / POST 追加
│  │     ├─ parts/[id]/route.ts       # GET / PATCH / DELETE
│  │     └─ parts/[id]/movements/route.ts  # POST 入出庫追加
│  ├─ lib/
│  │  ├─ db.ts                 # PrismaClient シングルトン
│  │  ├─ session.ts            # iron-session 設定（password 認証）
│  │  └─ stock.ts              # 在庫集計クエリ
│  ├─ middleware.ts            # /login と /api/login 以外は認証必須
│  └─ components/
│     ├─ ui/...                # shadcn/ui コンポーネント
│     ├─ PartTable.tsx
│     ├─ MovementDialog.tsx
│     └─ Nav.tsx
└─ scripts/
└─ backup.sh                # SQLite ファイルを日付付きでコピー（任意）

 ---
Railway デプロイ設定

- Railway プロジェクトを作成 → GitHub 連携で当リポジトリを指定。
- Volume: マウントパス /data（1GB で十分、無料枠内）。
- 環境変数:
    - DATABASE_URL=file:/data/inventory.db
    - APP_PASSWORD=<任意の強いパスワード>
    - SESSION_SECRET=<32文字以上のランダム文字列>
    - NODE_ENV=production
- ビルドコマンド: pnpm install && pnpm prisma migrate deploy && pnpm build
- 起動コマンド: pnpm start
- カスタムドメイン or Railway 提供の *.up.railway.app を利用。

    - カスタムドメイン or Railway 提供の *.up.railway.app を利用。

    ---
  実装ステップ（順序）

    1. Next.js + TypeScript + Tailwind 雛形作成（pnpm create next-app）
    2. Biome / shadcn/ui セットアップ
    3. Prisma 導入・schema.prisma 作成・初回マイグレーション
    4. lib/db.ts、lib/session.ts、middleware.ts で認証基盤
    5. ログイン画面とAPI
    6. 部品マスタCRUD（一覧・追加・編集・削除）
    7. 入出庫モーダルとAPI、現在在庫の集計表示
    8. 履歴画面とフィルタ
    9. 最低在庫アラート（一覧の警告バッジ）
    10. レスポンシブ調整・モバイル下タブ
    11. Railway 連携・Volume 設定・本番デプロイ
    12. スマホ実機での確認

    ---
  動作確認（受け入れ基準）

    - pnpm dev でローカル起動し、/login 経由で認証が機能する
    - 部品を新規登録できる（型番が重複すれば弾かれる）
    - 部品詳細から入庫・出庫を行うと現在在庫が即時更新される
    - 最低在庫を下回った部品が一覧で警告表示される
    - 履歴画面で期間・種別・部品でフィルタできる
    - スマホブラウザ（iPhone Safari / Android Chrome）でレイアウト崩れがない
    - Railway デプロイ後、再デプロイしてもDBが保持される（Volume確認）
    - 未ログイン状態で任意のページにアクセスすると /login にリダイレクトされる

    ---
  運用メモ

    - バックアップ: 月1回 scripts/backup.sh を手動実行、または Railway の railway run から cp /data/inventory.db /data/backup-YYYYMMDD.db を実行。
    - コスト目安: Railway Hobby プラン $5/月 + Volume $0.25/GB/月 ≒ 月 $5〜6。
    - 将来拡張: 複数ユーザー対応にする際は iron-session を NextAuth.js / Auth.js へ差し替え、Userテーブルを追加すればよい。
```

4) サンプルコードとして誰でも操作できるように、以下のプロンプトを入力。

入力1:
```
このリポジトリは単なるサンプルコードなので、本物の.envを作成してコミットして。実際に /use-railway でデプロイして。
```

入力2:
```
サンプルプログラムなので、パスワードをログイン画面に記載して、誰でもログインできるようにして
```

回答:
```
デプロイ結果

- 起動コマンド: npx prisma migrate deploy && npm start（マイグレーションをランタイムで実行＝ボリュームがマウントされた状態で行う）
- Sleep Application: 有効化（アイドル時にコンテナをスリープして課金を抑制／serverless 相当）
- 環境変数: DATABASE_URL, APP_PASSWORD, SESSION_SECRET, NODE_ENV=production

動作確認結果

- ✅ ログイン成功（/api/login → 200）
- ✅ 部品一覧表示（/ → 200、未登録メッセージ表示）
- ✅ 部品作成成功（DEMO-001 / テスト抵抗）
- ✅ 入出庫追加成功（+42個、理由「動作確認」）
- ✅ 再デプロイ後もデータが残存（在庫 42 個・履歴が維持）→ ボリューム永続化 OK

嵌った点（記録）

1. 初回は railway.json でビルド時に prisma migrate deploy を実行していたが、ビルド時の /data は永続ボリュームではないため DB が消えた。
2. preDeployCommand も別コンテナで実行され、ログ上「Mounting volume」より前に走るため永続化されない。
3. startCommand 内で migrate を実行するのが正解。ランタイムコンテナはボリュームがマウントされた状態で起動する。

スマホで上記 URL を開けばすぐ使えます。
```

6) 動作確認をして、Claude Codeと細かい質疑応答を通して、修正を反映。

入力1:
```
出庫がマイナスになっても大丈夫？
```

（回答は省略）

入力2:
```
一般論として負数を許容すべき？
```

回答1:
```
一般論としては 負数を許容すべき です。理由は3つ:
1. 現実とのズレを可視化できる:
実在庫より多く出庫記録が入っているのは「伝票漏れ・棚卸ミス・盗難」のシグナル。負数で残しておけば調査の手がかりになる。ゼロでブロックすると問題が隠蔽される。
2. 過去伝票の遡及入力ができる: 出庫を先に記録して入庫を後から入れる運用（締め処理・遅延報告）が成り立たなくなる。
3. 業務系 ERP（SAP/Oracle/Odoo 等）も既定で許容: 「Allow negative stock」フラグが普通にあり、製造業では ON が一般的。在庫不足は警告で示し、確定処理（棚卸調整伝票）で揃える。

ブロックすべきなのは「販売時にレジで在庫切れを売らせない」のような トランザクション境界が物理現実と即時同期している場合だけ で、自己管理の部品在庫は当てはまりません。

なので推奨は前回の (B) 警告だけ（マイナスは許容、UI で目立たせる）です。実装しますか？
```

入力3:
```
Bにして。また、数量の0をbackspaceで消せないので、入力バリデーションを維持しつつも、数量の入力欄を空にできるように改善して
```

回答2:
```
本番動作確認 OK。

変更まとめ（コミット 6039e3f）

負在庫の扱い
- API は超過する出庫もそのまま受理（伝票漏れ・棚卸ズレの可視化）
- 出庫ダイアログ: 数量が現在在庫を超えると黄色の警告文を表示し、送信前に「記録後は -XX 個になります。記録しますか？」と確認
- 部品詳細: マイナス時に赤帯バナー「在庫がマイナスです。入庫の記録漏れや棚卸のズレが疑われます」
- 部品一覧 / 詳細の在庫数: マイナス時は赤字＋赤バッジ「在庫マイナス」（最低在庫割れより強い表示）

入力欄の改善
- 出庫ダイアログの「数量」、部品フォームの「単価」「最低在庫」を Backspace で空欄にできるよう state の型を number | "" に変更
- 送信時バリデーション:
  - 数量・単価: 空欄なら送信不可（単価はエラーメッセージ表示）
  - 最低在庫: 空欄は 0 として正規化
- スマホで数字キーが出るよう inputMode="numeric" 付与

本番（https://inventory-manager-production-9b21.up.railway.app）で 42 個在庫から 100 個出庫 → -58 個になり、赤バナー・赤バッジが表示されることを確認しました。
```
